# 🎥 ビデオチュートリアル台本 / Video Tutorial Script

## 📋 概要 / Overview

このドキュメントは、FSx for NetApp ONTAP Embedding Batch Workloadのビデオチュートリアル作成用の台本です。

This document provides scripts for creating video tutorials for the FSx for NetApp ONTAP Embedding Batch Workload.

## 🎬 チュートリアル構成 / Tutorial Structure

### 📺 チュートリアル1: 概要とアーキテクチャ (5分)

#### タイトル: "FSx for NetApp ONTAP Embedding Batch Workload - 概要とアーキテクチャ"

**イントロ (30秒)**
```
こんにちは！今日は、Amazon FSx for NetApp ONTAPとAWS Batchを使用した
大規模文書埋め込み処理ワークロードについて説明します。

このソリューションは、エンタープライズグレードの高性能ストレージと
スケーラブルなコンピュート環境を組み合わせて、
AI/MLワークロードを効率的に処理します。
```

**アーキテクチャ説明 (2分)**
```
まず、システムアーキテクチャを見てみましょう。

[画面: アーキテクチャ図表示]

このシステムは4つの主要レイヤーで構成されています：

1. 入力レイヤー: S3とFSx for NetApp ONTAPからの文書読み込み
2. 処理レイヤー: AWS BatchとLambda関数による並列処理
3. AI統合: Amazon Bedrockによる埋め込み生成
4. ストレージレイヤー: DynamoDBとOpenSearchによる結果保存

特に注目すべきは、FSx for NetApp ONTAPの高性能ストレージにより、
大量の文書を高速に読み込めることです。
```

**主要機能紹介 (2分)**
```
このワークロードの主要機能を紹介します：

[画面: 機能一覧表示]

1. デュアルデプロイメント対応
   - CDKとCloudFormation両方に対応
   - 開発者と運用者、どちらにも最適

2. 権限ベースアクセス制御
   - ユーザーごとの文書アクセス権限
   - セキュアな処理環境

3. 自動スケーリング
   - 処理量に応じた動的リソース調整
   - コスト最適化

4. 包括的監視
   - CloudWatchダッシュボード
   - リアルタイムアラート
```

**まとめ (30秒)**
```
このワークロードにより、大規模な文書処理を
効率的かつ安全に実行できます。

次のビデオでは、実際のデプロイメント手順を
ステップバイステップで説明します。
```

### 📺 チュートリアル2: CDKデプロイメント (10分)

#### タイトル: "CDKを使用したデプロイメント - ステップバイステップ"

**イントロ (30秒)**
```
このビデオでは、AWS CDKを使用して
Embedding Batch Workloadをデプロイする方法を説明します。

CDKは、TypeScriptでインフラを定義できる
プログラマティックなアプローチです。
```

**前提条件確認 (1分)**
```
まず、前提条件を確認しましょう。

[画面: ターミナル表示]

# 前提条件チェックスクリプト実行
./scripts/check-prerequisites.sh

このスクリプトが、必要なツールがすべて
インストールされていることを確認します。
```

**設定ファイル作成 (2分)**
```
次に、設定ファイルを作成します。

[画面: エディタ表示]

# 基本設定をコピー
cp examples/basic-config.json config/my-config.json

設定ファイルでは、以下の項目を設定します：
- プロジェクト名
- 環境名
- AWSリージョン
- VPCとサブネット設定
- FSx設定
- Batch設定

実際の環境に合わせて、これらの値を編集してください。
```

**CDKデプロイメント (4分)**
```
設定が完了したら、デプロイメントを実行します。

[画面: ターミナル表示]

# CDKディレクトリに移動
cd cdk

# 依存関係インストール
npm install

# CDKブートストラップ（初回のみ）
npx cdk bootstrap

# デプロイメント実行
npx cdk deploy --require-approval never

デプロイメントには15-20分程度かかります。
進行状況はターミナルで確認できます。
```

**デプロイメント確認 (2分)**
```
デプロイメントが完了したら、結果を確認しましょう。

[画面: AWSコンソール表示]

CloudFormationコンソールで、作成されたスタックを確認できます。
また、各サービスのコンソールで個別のリソースも確認できます。

# コマンドラインでの確認
aws cloudformation describe-stacks --stack-name my-project-dev
```

**まとめ (30秒)**
```
CDKを使用することで、型安全で保守性の高い
インフラストラクチャを構築できました。

次のビデオでは、実際にジョブを実行して
結果を確認する方法を説明します。
```

### 📺 チュートリアル3: CloudFormationデプロイメント (8分)

#### タイトル: "CloudFormationを使用したデプロイメント"

**イントロ (30秒)**
```
このビデオでは、AWS CloudFormationを使用した
デプロイメント方法を説明します。

CloudFormationは、JSONまたはYAMLテンプレートで
インフラを宣言的に定義するAWS標準のサービスです。
```

**パラメータファイル準備 (2分)**
```
CloudFormationでは、パラメータファイルを使用して
環境固有の設定を管理します。

[画面: エディタ表示]

# 基本パラメータをコピー
cp examples/cloudformation/basic-parameters.json parameters/my-parameters.json

パラメータファイルでは、各設定項目を
ParameterKeyとParameterValueのペアで定義します。
```

**テンプレート検証 (1分)**
```
デプロイ前に、テンプレートを検証しましょう。

[画面: ターミナル表示]

# テンプレート検証
./scripts/validate-cloudformation.sh \
  --template cloudformation-templates/EmbeddingWorkloadStack.template.json \
  --parameters parameters/my-parameters.json

この検証により、構文エラーや設定ミスを
事前に発見できます。
```

**CloudFormationデプロイメント (3分)**
```
検証が完了したら、デプロイメントを実行します。

[画面: AWSコンソール表示]

AWSコンソールを使用する場合：
1. CloudFormationコンソールを開く
2. "Create Stack"をクリック
3. テンプレートファイルをアップロード
4. パラメータを入力
5. "Create Stack"を実行

コマンドラインを使用する場合：
# 統一デプロイメントスクリプト使用
./scripts/unified-deploy.sh --method cloudformation --env dev
```

**進行状況監視 (1分)**
```
デプロイメント中は、進行状況を監視できます。

[画面: CloudFormationコンソール表示]

CloudFormationコンソールの"Events"タブで、
リアルタイムの進行状況を確認できます。

また、監視スクリプトも使用できます：
./scripts/monitor-cloudformation.sh --stack my-project-dev --watch
```

**まとめ (30秒)**
```
CloudFormationを使用することで、
AWS標準のツールでインフラを管理できます。

特に運用チームや、厳格なガバナンスが必要な
環境では、CloudFormationが適しています。
```

### 📺 チュートリアル4: ジョブ実行と結果確認 (7分)

#### タイトル: "サンプルジョブの実行と結果確認"

**イントロ (30秒)**
```
このビデオでは、デプロイしたワークロードで
実際にジョブを実行し、結果を確認します。

文書処理から埋め込み生成まで、
一連の流れを体験しましょう。
```

**サンプルデータ準備 (1分30秒)**
```
まず、処理対象のサンプル文書を準備します。

[画面: エディタ表示]

# サンプル文書作成
mkdir sample-data
cat > sample-data/document1.txt << 'EOF'
Amazon FSx for NetApp ONTAP is a fully managed service...
EOF

実際の運用では、FSx for NetApp ONTAPに
大量の文書が保存されていることを想定しています。
```

**S3アップロード (1分)**
```
サンプル文書をS3にアップロードします。

[画面: ターミナル表示]

# S3バケット名取得
S3_BUCKET=$(aws cloudformation describe-stacks \
  --stack-name my-project-dev \
  --query 'Stacks[0].Outputs[?OutputKey==`S3BucketName`].OutputValue' \
  --output text)

# アップロード実行
aws s3 cp sample-data/ s3://$S3_BUCKET/input/ --recursive

アップロードが完了すると、Batchジョブで処理できます。
```

**Batchジョブ実行 (2分)**
```
いよいよBatchジョブを実行します。

[画面: ターミナル表示]

# ジョブ実行
JOB_ID=$(aws batch submit-job \
  --job-name "demo-job-$(date +%s)" \
  --job-queue $JOB_QUEUE \
  --job-definition $JOB_DEFINITION \
  --parameters inputPath="s3://$S3_BUCKET/input/",outputPath="s3://$S3_BUCKET/output/" \
  --query 'jobId' \
  --output text)

ジョブが実行されると、AWS Batchが自動的に
必要なコンピュートリソースを起動します。
```

**進行状況監視 (1分30秒)**
```
ジョブの進行状況を監視しましょう。

[画面: AWSコンソール表示]

AWS Batchコンソールで、ジョブの状態を確認できます：
- SUBMITTED: ジョブが送信された状態
- PENDING: リソース待ち
- RUNNABLE: 実行可能状態
- RUNNING: 実行中
- SUCCEEDED: 正常完了

コマンドラインでも確認できます：
aws batch describe-jobs --jobs $JOB_ID
```

**結果確認 (30秒)**
```
ジョブが完了したら、結果を確認します。

[画面: ターミナル表示]

# 結果ファイル確認
aws s3 ls s3://$S3_BUCKET/output/

# 結果ダウンロード
aws s3 cp s3://$S3_BUCKET/output/embeddings.json ./

# 埋め込み結果表示
cat embeddings.json | jq '.[0]'

生成された埋め込みベクトルが表示されます。
```

**まとめ (30秒)**
```
これで、文書から埋め込みベクトルを生成する
一連の流れが完了しました。

このワークロードを使用することで、
大規模な文書処理を効率的に実行できます。
```

### 📺 チュートリアル5: 監視とトラブルシューティング (6分)

#### タイトル: "監視、ログ確認、トラブルシューティング"

**イントロ (30秒)**
```
このビデオでは、デプロイしたワークロードの
監視方法とトラブルシューティングについて説明します。

運用において重要な、ログ確認やエラー対応の
方法を学びましょう。
```

**CloudWatchダッシュボード (2分)**
```
まず、CloudWatchダッシュボードを確認します。

[画面: CloudWatchコンソール表示]

デプロイメント時に自動作成されたダッシュボードでは、
以下のメトリクスを監視できます：

- ジョブ実行状況
- リソース使用率
- エラー発生状況
- コスト情報

カスタムメトリクスも追加できます。
```

**ログ確認 (2分)**
```
次に、ログの確認方法を説明します。

[画面: CloudWatchログコンソール表示]

主要なログソース：
1. AWS Batchジョブログ: /aws/batch/job
2. Lambda関数ログ: /aws/lambda/function-name
3. VPCフローログ: /aws/vpc/flowlogs

コマンドラインでの確認：
aws logs tail /aws/batch/job --follow

エラーログの検索：
aws logs filter-log-events \
  --log-group-name /aws/batch/job \
  --filter-pattern "ERROR"
```

**トラブルシューティング (2分30秒)**
```
よくある問題とその解決方法を説明します。

[画面: ターミナル表示]

問題1: FSxマウントエラー
# FSx状態確認
aws fsx describe-file-systems

解決方法：
- セキュリティグループでNFSポート(2049)を開放
- FSxファイルシステムがAVAILABLE状態であることを確認

問題2: Batchジョブ失敗
# ジョブ詳細確認
aws batch describe-jobs --jobs $JOB_ID

解決方法：
- IAMロールの権限確認
- リソース要件の調整
- ログでエラー詳細を確認

問題3: 権限エラー
# 権限シミュレーション
aws iam simulate-principal-policy

解決方法：
- 必要な権限をIAMロールに追加
- リソースベースポリシーの確認
```

**まとめ (30秒)**
```
適切な監視とログ確認により、
問題を早期に発見・解決できます。

包括的なトラブルシューティングガイドも
用意していますので、ぜひご活用ください。
```

### 📺 チュートリアル6: 本番環境への展開 (8分)

#### タイトル: "本番環境への展開とベストプラクティス"

**イントロ (30秒)**
```
このビデオでは、開発環境から本番環境への
展開方法とベストプラクティスを説明します。

エンタープライズグレードの設定と
セキュリティ要件について学びましょう。
```

**本番環境設定 (3分)**
```
本番環境では、より厳格な設定が必要です。

[画面: 設定ファイル表示]

主要な違い：
1. セキュリティ強化
   - KMS暗号化の有効化
   - VPCフローログ
   - GuardDuty統合

2. 高可用性
   - マルチAZ配置
   - 自動バックアップ
   - 災害復旧設定

3. 監視強化
   - 詳細監視の有効化
   - アラート設定
   - ダッシュボード作成

4. コスト最適化
   - スポットインスタンス
   - 自動スケーリング
   - リザーブドインスタンス
```

**段階的デプロイメント (2分)**
```
本番環境では、段階的なデプロイメントを推奨します。

[画面: デプロイメントフロー図表示]

1. 開発環境での検証
2. ステージング環境でのテスト
3. 本番環境への展開

各段階で、以下を確認：
- 機能テスト
- パフォーマンステスト
- セキュリティテスト
- 災害復旧テスト
```

**監視・運用 (2分)**
```
本番環境では、継続的な監視が重要です。

[画面: 監視ダッシュボード表示]

監視項目：
- システム稼働率
- ジョブ成功率
- リソース使用率
- セキュリティイベント
- コスト推移

アラート設定：
- 失敗ジョブの即座通知
- リソース使用率の閾値アラート
- セキュリティインシデント通知
```

**まとめ (30秒)**
```
本番環境では、セキュリティ、可用性、監視が
特に重要になります。

提供されているエンタープライズ設定例を
参考に、要件に応じてカスタマイズしてください。
```

## 🎬 撮影ガイドライン / Filming Guidelines

### 画面録画設定

- **解像度**: 1920x1080 (Full HD)
- **フレームレート**: 30fps
- **音声**: 48kHz, 16bit
- **録画ソフト**: OBS Studio, Camtasia, または同等

### 画面構成

```
┌─────────────────────────────────────────────────────────────┐
│ タイトル: チュートリアル名                                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────┐  ┌─────────────────────────────────┐   │
│  │                 │  │                                 │   │
│  │   ターミナル     │  │        AWSコンソール            │   │
│  │   または        │  │        または                   │   │
│  │   エディタ      │  │        図表・説明               │   │
│  │                 │  │                                 │   │
│  └─────────────────┘  └─────────────────────────────────┘   │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│ 進行状況バー: ████████░░ 80%                                │
└─────────────────────────────────────────────────────────────┘
```

### 音声ガイドライン

- **話速**: 150-180語/分
- **音量**: 一定レベル維持
- **発音**: 明瞭で聞き取りやすく
- **間**: 適切な間を取る

### 編集ポイント

- **カット**: 長い待機時間をカット
- **ズーム**: 重要な部分をズームイン
- **ハイライト**: 重要な設定項目をハイライト
- **字幕**: 重要なコマンドや設定値を字幕表示

## 📚 補足資料 / Supplementary Materials

### チュートリアル用ファイル

```bash
# チュートリアル用設定ファイル作成
mkdir -p examples/tutorials/configs

# 各チュートリアル用の設定
cp examples/basic-config.json examples/tutorials/configs/tutorial-basic.json
cp examples/enterprise-config.json examples/tutorials/configs/tutorial-enterprise.json
```

### デモスクリプト

```bash
# 各チュートリアル用のデモスクリプト
mkdir -p examples/tutorials/scripts

# チュートリアル2用CDKデモ
cat > examples/tutorials/scripts/cdk-demo.sh << 'EOF'
#!/bin/bash
# CDKチュートリアル用デモスクリプト
echo "CDKデプロイメントデモを開始します..."
# デモ用の簡略化されたコマンド
EOF

# チュートリアル3用CloudFormationデモ
cat > examples/tutorials/scripts/cloudformation-demo.sh << 'EOF'
#!/bin/bash
# CloudFormationチュートリアル用デモスクリプト
echo "CloudFormationデプロイメントデモを開始します..."
# デモ用の簡略化されたコマンド
EOF
```

## 🎯 チュートリアル完成チェックリスト

### 撮影前チェック

- [ ] 台本の確認・練習
- [ ] 必要なファイル・設定の準備
- [ ] 録画環境のテスト
- [ ] 音声品質の確認
- [ ] 画面解像度の設定

### 撮影後チェック

- [ ] 音声品質の確認
- [ ] 画面の見やすさ確認
- [ ] 内容の正確性確認
- [ ] 時間の適切性確認
- [ ] 字幕・ハイライトの追加

### 公開前チェック

- [ ] 最終品質確認
- [ ] 関連ドキュメントとの整合性
- [ ] アクセシビリティ対応
- [ ] 多言語対応（必要に応じて）

## 📞 制作サポート / Production Support

チュートリアル制作に関する質問：

- **技術的内容**: GitHub Issues
- **制作方法**: GitHub Discussions
- **品質確認**: プルリクエスト

---

**制作ガイド最終更新**: 2025年1月  
**対象バージョン**: v1.0.0  
**推定制作時間**: 2-3日